{
  "name": "wwdc-notes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch": "tsx scripts/fetch.ts",
    "summarize": "tsx scripts/summarize.ts",
    "notes": "npm run fetch && npm run summarize",
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "wrangler": "^3.80.0"
  }
}
